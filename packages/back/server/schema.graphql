type Query {
  user(id: ID!): User
}

type Mutation {
  createUser(username: String!, email: String!, password: String!): User
}

type User {
  id: ID!
  username: String!
  email: String!
  password: String!
}
