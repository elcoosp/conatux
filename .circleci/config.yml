version: 2
jobs:
  test:
    docker:
      - image: circleci/ruby:2.5.3
    steps:
      - run: npm run test:coverage
  build:
    docker:
      - image: circleci/ruby:2.5.3
    steps:
      - run: echo "Build"
  publish:
    docker:
      - image: circleci/ruby:2.5.3
    steps:
      - run: echo "Publish"
workflows:
  version: 2
  golive:
    jobs:
      - test
      - build
      - publish
  